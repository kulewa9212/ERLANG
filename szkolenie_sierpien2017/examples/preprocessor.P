-file("preprocessor.erl", 1).

-module(preprocessor).

-export([one/0]).

-record(whoah,{zero,one,two}).

one() ->
    Record = #whoah{zero = 0,one = 1,two = 2},
    io:format("~p~n", [Record]),
    ok.



